@model List<ReservaModel>
@{
    ViewData["Title"] = "Listar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="card">
  <div class="card-header">
    Lista Vuelos
  </div>
  <div class="card-body">
     
     <hr/>
     <table class = "table table-bordered">
         <thead>
             <tr>
                 <th>Codigo Reserva</th>
                 <th>Codigo Vuelo</th>
                 <th>Fecha Vuelo</th>
                 <th>Destino</th>
                 <th>Descripcion Vuelo</th>
                 <th>Ocupantes</th>
                 <th>Monto Total</th>
                 <th>Identificacion</th>
                 <th>Editar</th>
                 <th>Eliminar</th>
             </tr>
         </thead>
         <tbody>
             @foreach (var item in Model)
             {
                 <tr>
                     <td>@item.CodReserva</td>
                     <td>@item.CodVuelo</td>
                     <td>@item.FechaVuelo</td>
                     <td>@item.PaisDestino</td>
                     <td>@item.DescripcionDestino</td>
                     <td>@item.NumOcupantes</td>
                     <td>@item.MonTotal</td>
                     <td>@item.NomIdentificacion @item.ApeIdentificacion</td>

                     <td>
                          <a asp-action= "Editar" asp-controller="Reserva" class="btn btn-primary" asp-route-CodReserva= "@item.CodReserva">Editar</a>
                     </td>
                     <td>
                          <a asp-action= "Eliminar" asp-controller="Reserva" class="btn btn-danger" asp-route-CodReserva= "@item.CodReserva">Eliminar</a>
                     </td>
                 </tr>
                 
             }
         </tbody>
     </table>

  </div>
</div>

